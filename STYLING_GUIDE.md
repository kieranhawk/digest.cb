# 🎨 Руководство по стилям оформления дайджеста

## ✨ Добавленные элементы оформления

### 1. **Градиентные разделители между секциями**
Автоматически добавляются между всеми секциями. Имеют:
- Градиентный фон с паттерном
- Полупрозрачность
- Тени для объема

### 2. **Декоративные паттерны на фоне секций**
Тонкие радиальные градиенты добавляют глубину фону каждой секции.

---

## 📝 Как использовать новые стили

### 🔥 Выделение ключевых слов

**Используйте класс `.highlight-word` для важных слов:**

```html
<p>Продажи выросли на <span class="highlight-word">160%</span> за месяц!</p>
```

**Результат:** Слово будет выделено белым фоном с градиентом и рамкой.

---

### 💬 Блоки цитат

**Используйте класс `.quote-block` для цитат или важных высказываний:**

```html
<div class="quote-block">
    Мастер-класс стал эффективным инструментом продвижения продукта и заметно увеличил интерес к бренду в регионе.
    <cite>— Тоцкая Алёна, ОП Ярославль</cite>
</div>
```

**Результат:** Красивый блок с кавычками, градиентным фоном и автором.

---

### 📊 Выделение статистики в карточках

**Используйте класс `.key-stat` для важных цифр внутри карточек:**

```html
<p>Экономия составила <span class="key-stat">4 000 евро</span> на доставке.</p>
```

**Результат:** Цифра будет выделена красным акцентом с фоном.

---

## 🎯 Примеры применения

### Пример 1: Карточка с выделенными цифрами

```html
<div class="card slide-up">
    <article>
        <h4>Рост продаж Drinksome</h4>
        <p>Продажи выросли более чем в <span class="highlight-word">2 раза</span>, 
        рост составил <span class="key-stat">160%</span>.</p>
        
        <div class="quote-block">
            Мастер-класс превзошёл все ожидания!
            <cite>— Команда ОП Ярославль</cite>
        </div>
    </article>
</div>
```

### Пример 2: Секция с цитатой

```html
<section id="training" class="section">
    <div class="container">
        <h2>📚 Обучение и развитие</h2>
        
        <div class="quote-block">
            Аттестация показала высокий уровень вовлечённости и профессионального роста команды.
            <cite>— Результаты аттестации 2024</cite>
        </div>
        
        <p>Лучший результат: <span class="highlight-word">100 баллов</span></p>
    </div>
</section>
```

### Пример 3: Выделение в списках

```html
<ul>
    <li>Экономия <span class="key-stat">6 000 евро</span> на авто</li>
    <li>Ускорено получение на <span class="highlight-word">30 дней</span></li>
    <li>Снижение издержек на <span class="key-stat">10%</span></li>
</ul>
```

---

## 🎨 Визуальные эффекты

### Что добавлено автоматически:

1. **Между секциями:**
   - Градиентные разделители высотой 60px
   - Паттерн из повторяющихся линий
   - Полупрозрачность и размытие

2. **На фоне секций:**
   - Тонкие радиальные градиенты
   - Не мешают чтению
   - Добавляют глубину

3. **Стили для текста:**
   - `.highlight-word` - белое выделение для ключевых слов
   - `.quote-block` - блоки цитат с кавычками
   - `.key-stat` - красное выделение для статистики

---

## 💡 Рекомендации по использованию

### ✅ Используйте:
- `.highlight-word` для процентов, сроков, важных слов
- `.quote-block` для отзывов, важных заявлений, выводов
- `.key-stat` для денежных сумм, больших цифр, KPI

### ❌ Не используйте:
- Слишком много выделений в одном абзаце
- Цитаты для обычного текста
- Выделение каждого числа

### 🎯 Золотое правило:
Выделяйте только **самое важное** - 2-3 элемента на карточку максимум.

---

## 🚀 Быстрый старт

1. Откройте любую карточку в `index.html`
2. Найдите важные цифры или слова
3. Оберните их в `<span class="highlight-word">` или `<span class="key-stat">`
4. Для цитат создайте `<div class="quote-block">`
5. Сохраните и обновите страницу

**Готово!** Ваш дайджест стал визуально богаче! 🎉
